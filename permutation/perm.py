import os

def run_perm(domain, subdomains_array):
    # Placeholder for actual permutation logic
    print(f"Running permutation on {domain}:")
    for subdomain in subdomains_array:
        print(subdomain)
