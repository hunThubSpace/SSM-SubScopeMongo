import os

os.environ["PYTHONDONTWRITEBYTECODE"] = "1"

def run_sub_subfinder(subdomains):
    # Placeholder for actual subfinder logic
    print("Running subfinder on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])

def run_sub_crtsh(subdomains):
    # Placeholder for actual crtsh logic
    print("Running crtsh on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])
        
def run_sub_wayback(subdomains):
    # Placeholder for actual wayback logic
    print("Running wayback on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])
        
def run_sub_assetfinder(subdomains):
    # Placeholder for actual assetfinder logic
    print("Running assetfinder on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])
        
def run_sub_rapiddns(subdomains):
    # Placeholder for actual rapiddns logic
    print("Running rapiddns on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])
        
def run_sub_abuseipdb(subdomains):
    # Placeholder for actual abuseipdb logic
    print("Running abuseipdb on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])
        
def run_sub_shodan(subdomains):
    # Placeholder for actual crtsh logic
    print("Running crtsh on subdomains:")
    for subdomain in subdomains:
        print(subdomain['subdomain'])